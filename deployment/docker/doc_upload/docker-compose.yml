version: '3.8'
services:
  doc-upload:
    build:
      context: ../../..
      dockerfile: deployment/docker/doc_upload/Dockerfile
    container_name: pdf-autofillr-doc-upload
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
